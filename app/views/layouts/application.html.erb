<!DOCTYPE html>
<html>
  <head>
    <title>MoneyManagement</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    
    <header>
      <% if user_signed_in? %>
        <li>
          <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
        </li>
        
        <nav>
          <ul>
            <li>
              <%= link_to "カレンダー", users_show_path %>
            </li>
            <li>
              <%= link_to "今月の記録", new_month_record_path %>
            </li>
            <li>
              <%= link_to "今日の記録", new_day_record_path %>
            </li>
            <li>
              <%= link_to "各月収支一覧", month_records_path %>
            </li>
            <li>
              <%= link_to "週間支出一覧", day_records_path %>
            </li>
            <li>
              <%= link_to "ブックマーク", bookmarks_month_records_path %>
            </li>
          </ul>
        </nav>
      <% else %>
        <li>
          <%= link_to "新規登録", new_user_registration_path %>
        </li>
        <li>
          <%= link_to "ログイン", new_user_session_path %>
        </li>
      <% end %>
    </header>
    
    <%= yield %>
  </body>
</html>
